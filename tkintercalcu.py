from tkinter import *
from tkinter import ttk



calc = Tk()
calc.title('CALCULATOR')
calc.geometry('700x700')
calc.config(bg='#37db5d')

label = Label(calc, text='CALCULATOR', font=('Segeo UI', 40, 'bold'),bg='blue')
label.place(x=30, y=30, height=50, width=640)

text_area = Text(calc, font=('Segeo UI',20,'bold'))
text_area.place(x=30, y=110, height=60,width=640)


butto1 = 






calc.mainloop()